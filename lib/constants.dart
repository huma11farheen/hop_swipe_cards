enum TriggerDirection {
  none,
  rightOnCard,
  leftOnCard,
  rightOnProfile,
  leftOnProfile,
}

enum InProgressSwipingDirection {
  left,
  right,
  center,
}

enum CardSwipeOrientation {
  left,
  right,
  recover,
}
